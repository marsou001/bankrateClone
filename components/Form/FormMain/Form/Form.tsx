import styled from "styled-components";

const Form = styled.form`
    font-weight: 500;
    text-align: left;
    max-width: 300px;
    margin: auto;
    label, input, button {
        width: 100%;
    }
    input, button {
        margin: 8px 0;
        padding: 15px;
        border-radius: 3px;
    }

`;

export default Form;